apiVersion: v1
kind: ConfigMap
metadata:
  name: filebrowser-config
  namespace: filebrowser
data:
  config.yaml: |-
    server:
      sources:
        - path: "/data"
          name: data
          config:
            defaultEnabled: false
            createUserDir: true
