apiVersion: apps/v1
kind: Deployment
metadata:
  name: filebrowser
  labels:
    app: filebrowser
spec:
  replicas: 1
  selector:
    matchLabels:
      app: filebrowser
  template:
    metadata:
      labels:
        app: filebrowser
    spec:
      containers:
        - name: filebrowser
          image: docker.io/gtstef/filebrowser:1.3.3-stable
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - secretRef:
                name: filebrowser-auth
          env:
            - name: FILEBROWSER_DATABASE
              value: "/data/database.db"
            - name: FILEBROWSER_CONFIG
              value: "/etc/filebrowser/config.yaml"
          volumeMounts:
            - name: filebrowser-data
              mountPath: "/data"
            - name: config
              mountPath: "/etc/filebrowser/config.yaml"
              subPath: "config.yaml"
              readOnly: true
      volumes:
        - name: filebrowser-data
          persistentVolumeClaim:
            claimName: filebrowser-data
        - name: config
          configMap:
            name: filebrowser-config
