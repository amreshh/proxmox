apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: beyla
  namespace: lgtm
spec:
  interval: 10m
  chart:
    spec:
      chart: beyla
      version: "1.9.4"
      sourceRef:
        kind: HelmRepository
        name: grafana
      interval: 10m
