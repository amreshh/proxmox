apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/filebrowser

patches:
  - target:
      kind: HTTPRoute
      name: filebrowser
    patch: |-
      - op: add
        path: "/spec/hostnames"
        value:
          - filebrowser.kubernetes.home
