apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: immich
spec:
  instances: 1
  managed:
    - name: immich
      createdb: true
      superuser: true
      login: true
  storage:
    storageClass: retain
    size: 1Gi
