apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/victoria-metrics

patches:
  - target:
      kind: HTTPRoute
      name: grafana
    patch: |-
      - op: add
        path: "/spec/hostnames"
        value:
          - grafana.kubernetes.home
